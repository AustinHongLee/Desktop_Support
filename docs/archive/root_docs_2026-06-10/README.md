# Root Docs Archive 2026-06-10

> Status: historical root-level docs moved out of `docs/` to keep the workspace focused.

The active root-level construction document is:

```text
docs/iso_pdf_node_workflow_codex_handoff_2026-06-10.md
```

Everything in this folder was previously visible directly under `docs/`. These files are preserved for reference, but they should not be treated as active TODO unless a current handoff explicitly points back to them.
