# ISO PDF Historical Archive

> Status: historical reference only. These files are not active TODO lists.

Start with `../../iso_pdf_node_workflow_codex_handoff_2026-06-10.md` for the active node workflow construction state. Older root-level status and handoff files were moved to `../root_docs_2026-06-10/`.

This folder keeps the design record for the ISO PDF workbench uplift: audits, alternative plans, prompt specs, and architecture notes that led to the current implementation. They are preserved for context, but if any archived note conflicts with the completion handoff or current source code, treat the handoff and code as authoritative.

Node workflow drafts live under `node_workflow/` and are historical unless the active root handoff says otherwise.
